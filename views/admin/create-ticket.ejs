<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créer un ticket personnalisé</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .autocomplete-list {
            display: none;
            position: absolute;
            width: 100%;
            background: white;
            border: 1px solid #e2e8f0;
            border-top: none;
            border-radius: 0 0 0.375rem 0.375rem;
            max-height: 200px;
            overflow-y: auto;
            z-index: 1000;
        }
        .autocomplete-list.show { display: block; }
        .autocomplete-item {
            padding: 0.5rem 1rem;
            cursor: pointer;
        }
        .autocomplete-item:hover { background-color: #f3f4f6; }
        .autocomplete-item.selected { background-color: #e5e7eb; }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <main class="mt-8">
            <section class="bg-white p-6 rounded-lg shadow-md max-w-2xl mx-auto">
                <h2 class="text-xl font-semibold mb-4">Créer un ticket personnalisé</h2>
                <form action="/admin/create-ticket" method="POST" class="space-y-4">
                    <div class="form-group">
                        <label for="caller" class="block text-sm font-medium mb-1">Appelant:</label>
                        <input type="text" id="caller" name="caller" required class="w-full p-2 border rounded">
                    </div>

                    <div class="form-group">
                        <label for="reason" class="block text-sm font-medium mb-1">Raison:</label>
                        <textarea id="reason" name="reason" class="w-full p-2 border rounded"></textarea>
                    </div>

                    <div class="form-group">
                        <label for="tags" class="block text-sm font-medium mb-1">Tags (séparés par des virgules):</label>
                        <input type="text" id="tags" name="tags" class="w-full p-2 border rounded">
                    </div>

                    <div class="form-group">
                        <label for="status" class="block text-sm font-medium mb-1">Statut:</label>
                        <select id="status" name="status" class="w-full p-2 border rounded">
                            <option value="open">Ouvert</option>
                            <option value="closed">Fermé</option>
                        </select>
                    </div>

                    <div class="form-group flex items-center gap-2">
                        <input type="checkbox" id="isGLPI" name="isGLPI" value="true" class="rounded">
                        <label for="isGLPI" class="text-sm font-medium">GLPI:</label>
                    </div>

                    <div class="form-group">
                        <label for="createdAt" class="block text-sm font-medium mb-1">Date de création:</label>
                        <input type="datetime-local" id="createdAt" name="createdAt" class="w-full p-2 border rounded">
                    </div>

                    <div class="form-group">
                        <label for="createdBy" class="block text-sm font-medium mb-1">Créé par:</label>
                        <input type="text" id="createdBy" name="createdBy" value="Admin" class="w-full p-2 border rounded">
                    </div>

                    <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Créer le ticket</button>
                </form>
            </section>
        </main>
    </div>
</body>
</html>